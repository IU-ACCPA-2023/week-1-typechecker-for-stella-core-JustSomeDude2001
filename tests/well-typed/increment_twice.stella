// sample program in Stella Core
language core;

fn increment_twice(n : Nat) -> Nat {
  return succ(succ(n));
}

fn returner() -> (fn(Nat) -> Nat) {
    return increment_twice;
}

fn main(n : Nat) -> Nat {
  return returner()(n);
}

